# M1 - Audio Engine

## Status
Complete — core engine, ALSA/Dummy backends, plugin host, multichannel.

## Goals

- [x] ALSA audio backend (PCM playback, float/s16, auto device)
- [x] AudioBackend abstraction (pluggable interface)
- [x] Auto-detection: ALSA → Dummy fallback
- [ ] JACK / PipeWire backend implementation
- [ ] Real audio capture (AudioInputNode → system input)
- [x] Multichannel support (≥ 8 channels)
- [ ] Sample-accurate timing
- [ ] Transport control (play/stop/position)
- [ ] BPM / time signature management

## Exit Criteria

- [x] Audio plays through system output via ALSA
- [ ] Latency < 10ms end-to-end
- [x] Plugin instantiation via PluginHost works with real audio
- [x] Graph can be modified while audio thread is running (lock-free)
- [x] Session can be saved/loaded with active audio

## Current State

- **AudioBackend** interface: `init/start/stop/shutdown/running/name/set_process_callback`
- **ALSA backend**: opens `default` PCM, float or S16_LE, auto sample rate/channels/period size, `snd_pcm_writei` with xrun recovery
- **Dummy backend**: timer-driven (100% CPU-independent, for dev)
- **Auto backend selection**: `"auto"` type picks ALSA if compiled, else Dummy
- Audio I/O nodes: stubs (silent passthrough, ready for JACK wiring)
- Plugin ABI and host implemented
- Audio graph with topological sort processing
- **Multichannel**: MasterOutputNode configurable channel count, engine + ALSA zero all N channels, chimera-play `-c` flag
- **Serialization**: `release()` clears port vectors, factory supports 0-channel construction for deserialize

## Notable Fixes

- `AudioNode::release()` now clears port vectors (not just buffers), preventing port duplication on deserialize
- ALSA backend uses `config.audio_device` instead of `config.client_name` for PCM device name
- Engine audio callback zeros all `num_outputs` channels (not just stereo)
- Nodes added after `Engine::init()` are prepared (buffer allocation) before insertion into graph

## Next Actions

Carried to M2:
1. Wire AudioInputNode to system capture ports (ALSA capture)
2. Wire AudioOutputNode to system playback ports
3. Add transport state to engine
4. Implement sample-accurate parameter changes via control queue
5. JACK / PipeWire backend implementation
